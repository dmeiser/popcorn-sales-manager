/**
 * Global setup for vitest workspace
 * Ensures jsdom environment is properly initialized for all tests
 */

// This file ensures that jsdom globals are available
// when running vitest from the root directory
