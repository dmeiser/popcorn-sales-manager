# CDK Deployment Configuration
# Copy this file to .env and fill in your values
# IMPORTANT: .env is gitignored and should never be committed
#
# ⚠️ COPILOT IS FORBIDDEN FROM MODIFYING THIS FILE WITHOUT EXPLICIT USER AUTHORIZATION ⚠️
#
# Most values have sensible defaults - only override what you need.

# ============================================================================
# OPTIONAL: Override defaults
# ============================================================================

# Environment name (default: dev)
# ENVIRONMENT=dev

# AWS Region (default: us-east-1, or from AWS CLI profile)
# AWS_REGION=us-east-1

# Enable AppSync request/response logging (default: true)
# ENABLE_APPSYNC_LOGGING=true

# ============================================================================
# MIGRATION ONLY: Resource import (remove after migration is complete)
# ============================================================================
# These are only needed when importing existing AWS resources into CDK.
# Leave blank or remove entirely for fresh deployments.

# STATIC_BUCKET_NAME=
# EXPORTS_BUCKET_NAME=
# TABLE_NAME=
# USER_POOL_ID=
# APPSYNC_API_ID=

# ============================================================================
# TESTING ONLY: Test user credentials
# ============================================================================
# For integration tests against dev environment

# TEST_USER_EMAIL=test@example.com
# TEST_USER_PASSWORD=your-test-password
